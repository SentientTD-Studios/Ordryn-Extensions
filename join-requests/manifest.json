{
  "id": "join-requests",
  "name": "Join requests",
  "version": "1.0.0",
  "host_api": 1,
  "description": "Post new site join requests to one HTTPS webhook. Configured in Admin, not on every project channel.",
  "hooks": [
    { "on": "join.request" }
  ],
  "delivery": {
    "type": "http.webhook",
    "url_from": "webhook_url",
    "format": "json"
  },
  "settings": [
    {
      "key": "webhook_url",
      "type": "secret",
      "label": "Webhook URL",
      "description": "Site-level HTTPS URL. Join requests are never posted into project Slack/Discord channels.",
      "required": true,
      "scope": "site"
    }
  ],
  "templates": {
    "join.request": "New join request from {join_email}: {join_message}"
  }
}
